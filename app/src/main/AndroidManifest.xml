<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.provafirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bdsplit"
        android:label="BuddySplit"
        android:roundIcon="@mipmap/bdsplit_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProvaFIrebase">
        <activity android:name=".modificaspesa.ModificaSpesa"></activity>
        <activity android:name=".singleGroup.SingleSpesa" />
        <activity android:name=".singleGroup.SingleGroupActivity" />
        <activity android:name=".AddSpesa" />
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".GroupsActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>